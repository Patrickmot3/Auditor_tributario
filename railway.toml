[build]
builder = "NIXPACKS"

[deploy]
startCommand = "flask db upgrade && flask seed-db; gunicorn run:app --workers=2 --bind=0.0.0.0:$PORT"
healthcheckPath = "/api/v1/status"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
